---
import BaseLayout from "../../layouts/BaseLayout.astro";

const zhBase = `${import.meta.env.BASE_URL.replace(/\/$/, "")}/zh/`;
---

<BaseLayout
  title="集成入口"
  description="CodeArmy 通过 Trae、CLI、协作面和远端执行环境复用同一套 campaign state，但这些入口面的职责并不对等。"
>
  <section class="container page-hero">
    <p class="eyebrow">Integrations</p>
    <h1>同一套 campaign state，可以从多个入口进入，但主入口只有一个。</h1>
    <p class="lead">
      CodeArmy 的目标不是把所有入口面做成并列功能卡片，而是让同一个 campaign 能在不同工作面之间保持连续。
      日常使用以 Trae 为主；CLI、Feishu 和 Cluster / HPC 负责自动化、协作同步与远端执行。
    </p>
    <div class="hero-actions">
      <a class="button" href={`${zhBase}docs/quickstart/`}>回到中文 Quickstart</a>
      <a class="button-secondary" href={`${zhBase}how-it-works/`}>
        查看中文 How It Works
      </a>
    </div>
  </section>

  <section class="container section">
    <h2>入口层次</h2>
    <p class="section-copy">
      这些入口面共享的是 campaign 的事实、任务状态和结果沉淀，不代表它们都是产品的“第一现场”。
      如果把它们理解成完全等价的客户端，会误判当前产品边界。
    </p>
    <div class="grid-2">
      <article class="card">
        <p class="mini-title">Primary Surface</p>
        <h3>Trae Solo 是日常主入口</h3>
        <p>
          面向大多数日常编排、代码推进和任务切换。产品应主要通过 Trae 内的自定义 agents、Skills
          与 MCP tools 被使用，而不是要求用户先搭一套脚本系统。
        </p>
        <ul class="page-list">
          <li>适合开始一个新 campaign，持续推进 repo-first 执行流。</li>
          <li>最容易承接 quickstart、任务调度和多轮 review 的主叙事。</li>
          <li>当用户只需要一个入口面时，默认应优先理解为 Trae。</li>
        </ul>
      </article>
      <article class="card">
        <p class="mini-title">Shared State</p>
        <h3>共享的是状态，不是体验对等</h3>
        <p>
          同一个 campaign 可以跨入口继续执行，但不同 surface 承担的职责不同。共享 campaign
          state 的含义是任务、结果、审阅与调度上下文能回到同一套 repo-first 事实源，而不是每个入口都提供完整产品体验。
        </p>
        <ul class="page-list">
          <li>入口切换后，任务上下文不应脱离 campaign repo。</li>
          <li>结果、审阅和报告仍要回写到同一套 task package。</li>
          <li>次级入口可以有价值，但不应替代主入口的产品认知。</li>
        </ul>
      </article>
    </div>
  </section>

  <section class="container section">
    <h2>次级入口各自解决什么问题</h2>
    <p class="section-copy">
      CLI、Feishu 与 Cluster / HPC 都是重要的工作面，但都需要额外上下文、配置或外围系统配合。
      这里强调适用场景，而不是把它们包装成默认开箱即用的同级入口。
    </p>
    <div class="grid-2">
      <article class="card">
        <h3>CLI</h3>
        <p>
          更适合自动化任务、定时 reconcile、批量生成报告和脚本化接入。它是 automation-friendly 的 surface，
          不是所有用户的第一触点。
        </p>
        <ul class="page-list">
          <li>适用于脚本、CI 或计划任务中初始化和推进 campaign。</li>
          <li>适用于需要稳定复现的 repo-first 维护动作。</li>
          <li>通常要求用户已经理解 campaign contract，而非零门槛入口。</li>
        </ul>
      </article>
      <article class="card">
        <h3>Feishu</h3>
        <p>
          主要承担协作摘要、审批和外发通知，帮助人类团队接住运行结果。它是协作面，不是产品重心。
        </p>
        <ul class="page-list">
          <li>适合接收状态汇总、提醒和审批信号。</li>
          <li>适合把 repo 内结论同步给团队，而不是取代 repo 事实源。</li>
          <li>是否可用取决于接入环境与配置，不应被写成默认可直接使用。</li>
        </ul>
      </article>
      <article class="card">
        <h3>Cluster / HPC</h3>
        <p>
          真正跑实验、远端作业和大规模计算的执行面。CodeArmy 的价值在于远端结果仍能回到同一 campaign
          state，而不是把集群本身包装成产品前台。
        </p>
        <ul class="page-list">
          <li>适合研究实验、批量作业和远端资源调度。</li>
          <li>依赖集群环境、权限和站点侧脚本能力。</li>
          <li>没有这些外部前提时，不应被描述成开箱即用。</li>
        </ul>
      </article>
      <article class="card">
        <h3>边界提醒</h3>
        <p>
          中文页需要忠实表达当前证据边界。任何需要额外配置、站点私有环境或仍在演进中的能力，都只能作为接入方向或次级工作面说明，不能写成现成承诺。
        </p>
        <ul class="page-list">
          <li>不要把未验证成熟度的集成写成“默认支持”。</li>
          <li>不要暗示 Feishu 或集群能力对所有用户立即可用。</li>
          <li>不要把多入口理解为没有主次之分的 marketing 拼盘。</li>
        </ul>
      </article>
    </div>
  </section>

  <section class="container section">
    <div class="callout" style="padding: 1.5rem;">
      <p class="eyebrow">Recommended Path</p>
      <h2>先从中文 Quickstart 理解主入口，再回来看次级入口。</h2>
      <p class="section-copy" style="margin-bottom: 1rem;">
        如果你正在第一次接触 CodeArmy，优先走 Trae 主入口与 repo-first workflow。等你需要自动化、团队协作或远端执行时，再决定是否接入 CLI、Feishu 或 Cluster / HPC。
      </p>
      <div class="hero-actions" style="margin-top: 0;">
        <a class="button" href={`${zhBase}docs/quickstart/`}>打开中文 Quickstart</a>
        <a class="button-secondary" href={`${zhBase}how-it-works/`}>
          了解 workflow 与角色边界
        </a>
      </div>
    </div>
  </section>
</BaseLayout>
